logOutButton = connect(() => ({ children: 'Logout' }), { onclick: actionLogout })('button')
/*
function  LocalStoredReducer(localStorage, reducer) {
    if (state === undefined) {
        if (localStorageKey in localStorage) {
            try {
                
            } catch (e)

        }
    }
}*/